name: List Configs
on:
  workflow_dispatch:

jobs:
  list:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v3
        with:
          ref: patch-1

      - name: Find all defconfigs
        run: |
          find arch/arm64/configs/ -name "*_defconfig"
